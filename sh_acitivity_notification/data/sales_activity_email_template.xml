<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">   
 		<record id="template_mail_activity_due_notify_email" model="mail.template">
			<field name="name">Mail Activity Due - Send Email</field>
            <field name="subject">${object.res_model_id.display_name} # ${object.res_name} - Activity Due Notification</field>
            <field name="email_from"></field>
            <field name="email_to">${object.user_id.display_name}</field>
            <field name="model_id" ref="mail.model_mail_activity"/>
 			<field name="lang">${object.user_id.commercial_partner_id.lang}</field>			
            <field name="body_html">
            <![CDATA[
            <p>Dear ${object.user_id.display_name},
            </p>
			<p>
				Your Activity # ${object.activity_type_id.name} 
				% if object.summary 
				"${object.summary}"
				% endif
				scheduled on ${object.date_deadline} is past due.
			</p>
			
			<p>	
				% if object.note
				Note :
				${object.text_note}  
				% endif
            </p>

			<p>Thank you</p>

			]]></field>
        </record>	
	</data>
</odoo>	